"""Command line entry points for calibration tasks.

This package groups workflow helpers that orchestrate camera and hand-eye
calibration using the lower level algorithms.  They are exposed via the
``calibration-cli`` entry point defined in ``pyproject.toml``.
"""
